name: data
description: "A new Flutter package project."
version: 0.0.1
homepage:
publish_to: "none"

environment:
  sdk: ">=3.4.3 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  cloud_firestore: ^5.5.0
  graphql: ^5.2.0-beta.9
  http: ^1.2.2
  nb_utils: ^7.1.1
  web_socket_channel: ^3.0.3

  # Add the shared package as a dependency
  retry: ^3.1.2
  shared:
    path: ../shared

  # Add the initializer package as a dependency
  initializer:
    path: ../initializer

  gql: ^1.0.0
  amplify_flutter: ^2.6.4
  amplify_core: ^2.6.4
  amplify_api: ^2.6.4
  amplify_auth_cognito: ^2.6.4
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  graphql_codegen: ^1.0.1
  build_runner: ^2.4.13
flutter:
  # Assets for the data module

  # If you need to add assets or fonts to your package, you can do so here.
  # For example:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
